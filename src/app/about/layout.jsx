export const metadata = {
  title: "About Us",
  description:
    "Learn about the history, vision, and team behind NextHaven's luxury hotel suites.",
};

export default function AboutLayout({ children }) {
  return <>{children}</>;
}
