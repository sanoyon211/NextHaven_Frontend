export const metadata = {
  title: "Restaurant & Dining",
  description: "Experience world-class culinary delights at NextHaven's exclusive restaurant.",
};

export default function RestaurantLayout({ children }) {
  return <>{children}</>;
}
