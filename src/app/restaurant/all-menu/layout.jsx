export const metadata = {
  title: "Full Menu",
  description: "Browse the complete dining and drinks menu available at NextHaven.",
};

export default function AllMenuLayout({ children }) {
  return <>{children}</>;
}
