export const metadata = {
  title: "Guest Dashboard",
  description:
    "Manage your bookings, room orders, and preferences in your NextHaven dashboard.",
};

export default function DashboardLayout({ children }) {
  return <>{children}</>;
}
