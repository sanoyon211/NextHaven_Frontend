export const metadata = {
  title: "Contact Us",
  description: "Get in touch with the NextHaven team for inquiries, reservations, or support.",
};

export default function ContactLayout({ children }) {
  return <>{children}</>;
}
